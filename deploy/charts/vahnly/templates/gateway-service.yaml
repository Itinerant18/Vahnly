apiVersion: v1
kind: Service
metadata:
  name: {{ include "vahnly.name" . }}-gateway-svc
  labels:
    app: gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: gateway
