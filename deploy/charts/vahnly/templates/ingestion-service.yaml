apiVersion: v1
kind: Service
metadata:
  name: {{ include "drivers-for-u.name" . }}-ingestion-svc
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: grpc
      name: grpc
    - port: 8080
      targetPort: metrics
      name: metrics
  selector:
    app: ingestion
