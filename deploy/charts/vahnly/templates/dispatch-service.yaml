apiVersion: v1
kind: Service
metadata:
  name: {{ include "vahnly.name" . }}-dispatch-svc
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: metrics
      name: metrics
  selector:
    app: dispatch
