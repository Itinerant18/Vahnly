apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vahnly.name" . }}-pricing
  labels:
    app: pricing
spec:
  replicas: {{ .Values.pricing.replicaCount }}
  selector:
    matchLabels:
      app: pricing
  template:
    metadata:
      labels:
        app: pricing
    spec:
      containers:
        - name: pricing
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- include "vahnly.securityContext" . | nindent 12 }}
          command: ["/entrypoint"]
          env:
            - name: KAFKA_BROKERS
              value: {{ .Values.global.env.kafkaBrokers | quote }}
            - name: REDIS_CLUSTER_ADDRS
              value: {{ .Values.global.env.redisClusterNodes | quote }}
            {{- include "vahnly.redisAuthEnv" . | nindent 12 }}
            - name: PRICING_GROUP_ID
              value: {{ .Values.pricing.groupID | quote }}
            {{- include "vahnly.kafkaSecurityEnv" . | nindent 12 }}
          resources:
            {{- toYaml .Values.pricing.resources | nindent 12 }}
