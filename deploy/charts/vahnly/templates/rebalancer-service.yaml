apiVersion: v1
kind: Service
metadata:
  name: {{ include "vahnly.name" . }}-rebalancer-svc
  labels:
    app: rebalancer
spec:
  type: ClusterIP
  ports:
    - port: 8090
      targetPort: http
      name: http
  selector:
    app: rebalancer
