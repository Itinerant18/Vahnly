{
  "name": "drivers-for-u-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-app Playwright e2e + visual-regression suite for the Drivers-for-u frontends.",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "update-snapshots": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
