networks:
  dispatch-tier:
    name: dispatch_network
    driver: bridge

services:
  # ============================================================================
  # 1. RELATIONAL SPATIAL STORE (POSTGRESQL + POSTGIS)
  # ============================================================================
  spatial-db:
    image: postgis/postgis:15-3.3
    container_name: spatial_postgres_dev
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=delivery_platform
    ports:
      - "5432:5432"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ============================================================================
  # 1b. OBJECT STORAGE (MinIO — S3-compatible, local dev for KYC/trip/support docs)
  # ============================================================================
  minio:
    image: minio/minio:latest
    container_name: dispatch_minio_dev
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD-SHELL", "mc ready local || curl -f http://localhost:9000/minio/health/live || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5

  # One-shot bucket bootstrap: creates the documents/trip-photos/rider-documents/support-attachments buckets.
  minio-init:
    image: minio/mc:latest
    container_name: dispatch_minio_init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin123 &&
      mc mb -p local/documents local/trip-photos local/rider-documents local/support-attachments &&
      echo 'MinIO buckets ready' "
    networks:
      - dispatch-tier

  # ============================================================================
  # 2. EVENT BACKBONE BROKER (KAFKA IN KRAFT MODE)
  # ============================================================================
  kafka-broker:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka_kraft_dev
    ports:
      - "9092:9092"
      - "19092:19092"
    environment:
      - KAFKA_NODE_ID=1
      - KAFKA_PROCESS_ROLES=broker,controller
      - KAFKA_CONTROLLER_QUORUM_VOTERS=1@kafka-broker:9093
      - KAFKA_LISTENERS=PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093,EXTERNAL://0.0.0.0:19092
      - KAFKA_INTER_BROKER_LISTENER_NAME=PLAINTEXT
      - KAFKA_CONTROLLER_LISTENER_NAMES=CONTROLLER
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,EXTERNAL:PLAINTEXT
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka-broker:9092,EXTERNAL://127.0.0.1:19092
      - KAFKA_LOG_DIRS=/tmp/kraft-combined-logs
      - CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
      - KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS=0
      - KAFKA_TRANSACTION_STATE_LOG_MIN_ISR=1
      - KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR=1
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:9092 --list"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ============================================================================
  # 3. HIGH-VELOCITY INTELLIGENCE TIER (NVIDIA TRITON SERVER)
  # ============================================================================
  triton-server:
    image: nvcr.io/nvidia/tritonserver:24.01-py3
    container_name: triton_inference_dev
    command: ["tritonserver", "--model-repository=/models"]
    ports:
      - "8000:8000"
      - "8001:8001" # Target gRPC Port binding for your ETACorrector Client
      - "8002:8002"
    volumes:
      - ./model_repository:/models
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/v2/health/ready || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 3

  # ============================================================================
  # 4. MEMORY STORAGE GRID (6-NODE SHARDED REDIS CLUSTER)
  # ============================================================================
  redis-node-1:
    image: redis:7.2-alpine
    container_name: driver-redis-node-1
    command: ["redis-server", "--cluster-enabled", "yes", "--cluster-config-file", "nodes.conf", "--cluster-node-timeout", "5000", "--appendonly", "yes", "--protected-mode", "no"]
    ports:
      - "6379:6379"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis-node-2:
    image: redis:7.2-alpine
    container_name: driver-redis-node-2
    command: ["redis-server", "--cluster-enabled", "yes", "--cluster-config-file", "nodes.conf", "--cluster-node-timeout", "5000", "--appendonly", "yes", "--protected-mode", "no"]
    ports:
      - "6380:6379"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis-node-3:
    image: redis:7.2-alpine
    container_name: driver-redis-node-3
    command: ["redis-server", "--cluster-enabled", "yes", "--cluster-config-file", "nodes.conf", "--cluster-node-timeout", "5000", "--appendonly", "yes", "--protected-mode", "no"]
    ports:
      - "6381:6379"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis-node-4:
    image: redis:7.2-alpine
    container_name: driver-redis-node-4
    command: ["redis-server", "--cluster-enabled", "yes", "--cluster-config-file", "nodes.conf", "--cluster-node-timeout", "5000", "--appendonly", "yes", "--protected-mode", "no"]
    ports:
      - "6382:6379"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis-node-5:
    image: redis:7.2-alpine
    container_name: driver-redis-node-5
    command: ["redis-server", "--cluster-enabled", "yes", "--cluster-config-file", "nodes.conf", "--cluster-node-timeout", "5000", "--appendonly", "yes", "--protected-mode", "no"]
    ports:
      - "6383:6379"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  redis-node-6:
    image: redis:7.2-alpine
    container_name: driver-redis-node-6
    command: ["redis-server", "--cluster-enabled", "yes", "--cluster-config-file", "nodes.conf", "--cluster-node-timeout", "5000", "--appendonly", "yes", "--protected-mode", "no"]
    ports:
      - "6384:6379"
    networks:
      - dispatch-tier
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Automation service that initializes cluster state across nodes on startup
  redis-cluster-orchestrator:
    image: redis:7.2-alpine
    container_name: redis_cluster_bootstrap
    depends_on:
      redis-node-1:
        condition: service_healthy
      redis-node-2:
        condition: service_healthy
      redis-node-3:
        condition: service_healthy
      redis-node-4:
        condition: service_healthy
      redis-node-5:
        condition: service_healthy
      redis-node-6:
        condition: service_healthy
    networks:
      - dispatch-tier
    entrypoint: [ "/bin/sh", "-c" ]
    command:
      - |
        apk add --no-cache bind-tools
        get_ip() { idx=0; while [ $$idx -lt 10 ]; do ip=$$(getent hosts driver-redis-node-$$1 | awk '{print $$1}'); if [ ! -z "$$ip" ]; then echo "$$ip"; return 0; fi; sleep 1; idx=$$((idx+1)); done; return 1; }
        
        # Build node endpoint array map
        NODES=""
        for i in 1 2 3 4 5 6; do
          ip=$$(get_ip $$i)
          NODES="$$NODES $$ip:6379"
        done
        
        echo "Initializing 6-Shard Redis Topology across nodes: $$NODES"
        echo "yes" | redis-cli --cluster create $$NODES --cluster-replicas 1
        echo "Cluster handshake complete."

  # ============================================================================
  # 5. SCHEMA MIGRATIONS — single source of truth (golang-migrate)
  # Runs all pending up-migrations against the DB, then exits. Every DB-backed
  # service waits for this to complete before starting.
  # ============================================================================
  db-migrator:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: migrate
    container_name: dispatch_db_migrator
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
    depends_on:
      spatial-db:
        condition: service_healthy
    restart: "no"
    networks:
      - dispatch-tier

  reconciler-daemon:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: reconciler
    container_name: dispatch_reconciler_prod
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
      - KAFKA_BROKERS=kafka-broker:9092
      - CITY_PREFIX=KOL
    depends_on:
      spatial-db:
        condition: service_healthy
      kafka-broker:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  telemetry-ingestion:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: ingestion
    container_name: dispatch_ingestion_prod
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
      - KAFKA_BROKERS=kafka-broker:9092
      - REDIS_CLUSTER_NODES=redis-node-1:6379
    ports:
      - "50051:50051"
    depends_on:
      spatial-db:
        condition: service_healthy
      kafka-broker:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  matching-engine:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: dispatch
    container_name: dispatch_matcher_prod
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
      - KAFKA_BROKERS=kafka-broker:9092
      - REDIS_CLUSTER_NODES=redis-node-1:6379
      - TRITON_SERVER_URL=triton-server:8001
      - ALGORITHM_STRATEGY=GREEDY
    depends_on:
      spatial-db:
        condition: service_healthy
      kafka-broker:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
      triton-server:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  surge-engine:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: surge
    container_name: dispatch_surge_prod
    environment:
      - KAFKA_BROKERS=kafka-broker:9092
      - REDIS_CLUSTER_NODES=redis-node-1:6379
      - SURGE_CITY_PREFIX=KOL
      - SURGE_TRACKED_CELLS=88754cb247fffff,88283473fffffff
    depends_on:
      kafka-broker:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  pricing-sync:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: pricing
    container_name: dispatch_pricing_prod
    environment:
      - KAFKA_BROKERS=kafka-broker:9092
      - REDIS_CLUSTER_ADDRS=redis-node-1:6379
      - PRICING_GROUP_ID=pricing-service-consumer-group
    depends_on:
      kafka-broker:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  telemetry-pruner:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: pruner
    container_name: dispatch_pruner_prod
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
      - REDIS_CLUSTER_NODES=redis-node-1:6379
    depends_on:
      spatial-db:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  offer-expiry-janitor:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: expiry
    container_name: dispatch_expiry_janitor_prod
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
      - REDIS_CLUSTER_NODES=redis-node-1:6379
      - KAFKA_BROKERS=kafka-broker:9092
      - CITY_PREFIX=KOL
    depends_on:
      spatial-db:
        condition: service_healthy
      kafka-broker:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  spatial-analytics:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: analytics
    container_name: dispatch_spatial_analytics_prod
    ports:
      - "8089:8089"
    environment:
      - KAFKA_BROKERS=kafka-broker:9092
      - CITY_PREFIX=KOL
      - ANALYTICS_PORT=8089
    depends_on:
      kafka-broker:
        condition: service_healthy
    networks:
      - dispatch-tier

  outbox-notification-engine:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: notification
    container_name: dispatch_notification_prod
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
    depends_on:
      spatial-db:
        condition: service_healthy
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  public-gateway:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: gateway
    container_name: dispatch_gateway_prod
    ports:
      - "8085:8080"
    environment:
      - DATABASE_URL=postgres://postgres:password@spatial-db:5432/delivery_platform?sslmode=disable
      - REDIS_CLUSTER_NODES=redis-node-1:6379
      - KAFKA_BROKERS=kafka-broker:9092
      - HTTP_PORT=8080
      - JWT_SECRET_SIGNING_KEY=${JWT_SECRET_SIGNING_KEY:?set JWT_SECRET_SIGNING_KEY in .env}
      - FIELD_ENCRYPTION_KEY=${FIELD_ENCRYPTION_KEY}
      - SUPPORTED_REGIONS_MATRIX=${SUPPORTED_REGIONS_MATRIX:-}
      # Dev only: allow KYC uploads to fall back to ephemeral local disk when S3 is
      # unconfigured. Unset/false in production → gateway refuses to boot without S3.
      - ALLOW_LOCAL_OBJECT_STORE=${ALLOW_LOCAL_OBJECT_STORE:-false}
      # CORS allow-list (comma-separated). No wildcard in production.
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-}
      # Admin session cookie SameSite. Default Lax (same-origin). Set =none for a cross-site
      # SPA→API deployment (Firebase Hosting frontend + separate API origin); None requires
      # Secure, so keep the gateway on HTTPS.
      - ADMIN_COOKIE_SAMESITE=${ADMIN_COOKIE_SAMESITE:-}
      # S3-compatible object storage (MinIO in local dev).
      - S3_ENDPOINT=${S3_ENDPOINT:-http://minio:9000}
      - S3_BUCKET=${S3_BUCKET_DOCUMENTS:-documents}
      - S3_FORCE_PATH_STYLE=true
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-minioadmin}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-minioadmin123}
    depends_on:
      spatial-db:
        condition: service_healthy
      minio:
        condition: service_healthy
      kafka-broker:
        condition: service_healthy
      redis-cluster-orchestrator:
        condition: service_completed_successfully
      db-migrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

  fleet-rebalancer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TARGET_SERVICE: rebalancer
    container_name: dispatch_rebalancer_prod
    environment:
      - REDIS_CLUSTER_NODES=redis-node-1:6379
      - CITY_PREFIX=KOL
    depends_on:
      redis-cluster-orchestrator:
        condition: service_completed_successfully
    networks:
      - dispatch-tier

volumes:
  minio_data:
